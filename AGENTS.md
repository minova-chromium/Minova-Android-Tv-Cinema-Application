# Minova Cinema repository instructions

Before preparing or publishing any Minova Cinema release, read and follow `RELEASING.md` completely.

Never change the application ID or rotate the release signing key. Every public APK must use a strictly higher version code, the established signing certificate, and the asset name `Minova-Cinema-<version>.apk`; otherwise installed copies cannot update automatically.
