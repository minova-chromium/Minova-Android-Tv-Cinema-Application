<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The user chooses a local Plex host at runtime, so LAN HTTP must be allowed. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
